// WARNING: DO NOT EDIT THIS FILE.
// You may push code into the target .java compilation unit if you wish to edit any member(s).
package org.openlegacy.as400.menus.web;

import java.util.*;
import javax.inject.Inject;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.commons.io.IOUtils;
import org.openlegacy.as400.menus.*;

import java.io.IOException;
import org.openlegacy.modules.table.Table;
import org.openlegacy.modules.table.TableWriter;
import java.net.MalformedURLException;
import java.net.URL;
import java.text.SimpleDateFormat;
import org.springframework.beans.propertyeditors.CustomDateEditor;

import org.openlegacy.terminal.ScreenEntity;
import org.openlegacy.terminal.TerminalSession;
import org.openlegacy.terminal.actions.TerminalActions;
import org.openlegacy.terminal.services.ScreenEntitiesRegistry;
import org.openlegacy.terminal.json.JsonSerializationUtil;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.InitBinder;
import org.springframework.web.bind.WebDataBinder;

import org.openlegacy.as400.menus.SpecifyLibrariesToWorkWith;

privileged @SuppressWarnings("unused") aspect SpecifyLibrariesToWorkWithController_Aspect {

	@Inject
	private TerminalSession SpecifyLibrariesToWorkWithController.terminalSession;

	@Inject
	private ScreenEntitiesRegistry SpecifyLibrariesToWorkWithController.screenEntitiesRegistry;

	
	// handle page initial display
    @RequestMapping(method = RequestMethod.GET, params="partial=1")
    public String SpecifyLibrariesToWorkWithController.show(Model uiModel) {
    	SpecifyLibrariesToWorkWith specifyLibrariesToWorkWith = terminalSession.getEntity(SpecifyLibrariesToWorkWith.class);
	uiModel.addAttribute("specifyLibrariesToWorkWith", specifyLibrariesToWorkWith);
	// show the resulting page for SpecifyLibrariesToWorkWith
        return "SpecifyLibrariesToWorkWith";
    }

	
    @RequestMapping(value="/help", method = RequestMethod.GET)
    public @ResponseBody String SpecifyLibrariesToWorkWithController.systemHelp(HttpServletRequest request) throws IOException {
    	URL resource = request.getSession().getServletContext().getResource("/help/SpecifyLibrariesToWorkWith.html");
    	String result = "";
    	if (resource != null){
    		result = IOUtils.toString(resource.openStream());
    	}
    	return result;
    }

	// handle submit action
    @RequestMapping(method = RequestMethod.POST)
    public String SpecifyLibrariesToWorkWithController.submit(SpecifyLibrariesToWorkWith specifyLibrariesToWorkWith, Model uiModel, HttpServletRequest httpServletRequest) {
	ScreenEntity resultScreenEntity = terminalSession.doAction(TerminalActions.ENTER(), specifyLibrariesToWorkWith);
	// go to the controller for the resulting screen name
		if (resultScreenEntity != null){
			String screenEntityName = screenEntitiesRegistry.get(resultScreenEntity.getClass()).getEntityClassName();
			if (httpServletRequest.getParameter("partial") != null){
				specifyLibrariesToWorkWith = terminalSession.getEntity(SpecifyLibrariesToWorkWith.class);
		    	uiModel.addAttribute("specifyLibrariesToWorkWith", specifyLibrariesToWorkWith);
		        return "SpecifyLibrariesToWorkWith";
			}
        	return "redirect:" + screenEntityName;
		}
    	return "redirect:/";
		
    }
    
	// handle exit action
    @RequestMapping(params="action=exit", method = RequestMethod.POST)
    public String SpecifyLibrariesToWorkWithController.exit(SpecifyLibrariesToWorkWith specifyLibrariesToWorkWith, Model uiModel, HttpServletRequest httpServletRequest) {
	ScreenEntity resultScreenEntity = terminalSession.doAction(TerminalActions.F3(), specifyLibrariesToWorkWith);
	// go to the controller for the resulting screen name
		if (resultScreenEntity != null){
			String screenEntityName = screenEntitiesRegistry.get(resultScreenEntity.getClass()).getEntityClassName();
			if (httpServletRequest.getParameter("partial") != null){
				specifyLibrariesToWorkWith = terminalSession.getEntity(SpecifyLibrariesToWorkWith.class);
		    	uiModel.addAttribute("specifyLibrariesToWorkWith", specifyLibrariesToWorkWith);
		        return "SpecifyLibrariesToWorkWith";
			}
        	return "redirect:" + screenEntityName;
		}
    	return "redirect:/";
		
    }
	// handle refresh action
    @RequestMapping(params="action=refresh", method = RequestMethod.POST)
    public String SpecifyLibrariesToWorkWithController.refresh(SpecifyLibrariesToWorkWith specifyLibrariesToWorkWith, Model uiModel, HttpServletRequest httpServletRequest) {
	ScreenEntity resultScreenEntity = terminalSession.doAction(TerminalActions.F5(), specifyLibrariesToWorkWith);
	// go to the controller for the resulting screen name
		if (resultScreenEntity != null){
			String screenEntityName = screenEntitiesRegistry.get(resultScreenEntity.getClass()).getEntityClassName();
			if (httpServletRequest.getParameter("partial") != null){
				specifyLibrariesToWorkWith = terminalSession.getEntity(SpecifyLibrariesToWorkWith.class);
		    	uiModel.addAttribute("specifyLibrariesToWorkWith", specifyLibrariesToWorkWith);
		        return "SpecifyLibrariesToWorkWith";
			}
        	return "redirect:" + screenEntityName;
		}
    	return "redirect:/";
		
    }
	// handle cancel action
    @RequestMapping(params="action=cancel", method = RequestMethod.POST)
    public String SpecifyLibrariesToWorkWithController.cancel(SpecifyLibrariesToWorkWith specifyLibrariesToWorkWith, Model uiModel, HttpServletRequest httpServletRequest) {
	ScreenEntity resultScreenEntity = terminalSession.doAction(TerminalActions.F12(), specifyLibrariesToWorkWith);
	// go to the controller for the resulting screen name
		if (resultScreenEntity != null){
			String screenEntityName = screenEntitiesRegistry.get(resultScreenEntity.getClass()).getEntityClassName();
			if (httpServletRequest.getParameter("partial") != null){
				specifyLibrariesToWorkWith = terminalSession.getEntity(SpecifyLibrariesToWorkWith.class);
		    	uiModel.addAttribute("specifyLibrariesToWorkWith", specifyLibrariesToWorkWith);
		        return "SpecifyLibrariesToWorkWith";
			}
        	return "redirect:" + screenEntityName;
		}
    	return "redirect:/";
		
    }
	
	
	@InitBinder
	public void SpecifyLibrariesToWorkWithController.initBinder(WebDataBinder binder) {
		SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd");
		dateFormat.setLenient(false);
		binder.registerCustomEditor(Date.class, new CustomDateEditor(dateFormat, false));
	}
	
}
